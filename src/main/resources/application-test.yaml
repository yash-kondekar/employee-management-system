spring:
  application:
    name: employee-management-system-test

  datasource:
    url: jdbc:postgresql://localhost:5432/ems_test_db
    username: postgres
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: true

    properties:
      hibernate:
        format_sql: true